package MomentoDP;


/*
*
* Maily used in Use cases when we need to take Snapshot of a object and revert/undo it to some previous state if required.
* Thus its also called Snapshot DP and mainly used if we want to save the history of states of an object.
* Also thus Maily used for the undo() capability for objects
*
* */
public class Client {
}
